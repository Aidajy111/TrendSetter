FROM node:22-alpine as build

WORKDIR /app

COPY package*.json ./

RUN npm install || echo "No package.json found"

COPY . .

ARG REACT_APP_API_URL
ENV REACT_APP_API_URL=$REACT_APP_API_URL

RUN npm run build || echo "No build script found"

FROM nginx:alpine


COPY --from=build /app/dist /usr/share/nginx/html

# Если ты используешь React Router, нужна небольшая настройка Nginx, 
# чтобы при обновлении страницы не выдавало ошибку 404. 
# (Раскомментируй строку ниже и создай файл nginx.conf, если это потребуется позже)
# COPY nginx.conf /etc/nginx/conf.d/default.conf

# Открываем 80 порт внутри контейнера
EXPOSE 80

# Запускаем Nginx
CMD ["nginx", "-g", "daemon off;"]